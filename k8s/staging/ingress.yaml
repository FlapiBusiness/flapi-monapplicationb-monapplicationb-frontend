apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-monapplicationb-monapplicationb-frontend-ingress
  namespace: staging-monapplicationb-monapplicationb-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.monapplicationb.flapi.org
      secretName: staging-monapplicationb-monapplicationb-frontend-tls
  rules:
    - host: staging.monapplicationb.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: staging-monapplicationb-monapplicationb-frontend-service
                port:
                  number: 3000
