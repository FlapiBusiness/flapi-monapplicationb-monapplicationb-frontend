apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-monapplicationb-monapplicationb-frontend-deployment
  namespace: prod-monapplicationb-monapplicationb-frontend
  labels:
    app: prod-monapplicationb-monapplicationb-frontend
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: prod-monapplicationb-monapplicationb-frontend
  template:
    metadata:
      labels:
        app: prod-monapplicationb-monapplicationb-frontend
    spec:
      containers:
        - name: prod-monapplicationb-monapplicationb-frontend
          image: corentin35/monapplicationb-monapplicationb-frontend:${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: '1'
              memory: '1Gi'
            limits:
              cpu: '2'
              memory: '2Gi'
      imagePullSecrets:
        - name: regcred
