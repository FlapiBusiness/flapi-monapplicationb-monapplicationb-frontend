apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-monapplicationb-monapplicationb-frontend-ingress
  namespace: prod-monapplicationb-monapplicationb-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - monapplicationb.flapi.org
      secretName: prod-monapplicationb-monapplicationb-frontend-tls
  rules:
    - host: monapplicationb.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: prod-monapplicationb-monapplicationb-frontend-service
                port:
                  number: 3000
